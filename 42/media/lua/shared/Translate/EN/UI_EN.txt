UI_EN = {
    UI_CAA_name = "[B42] Controller Aim Assist",
    UI_CAA_desc = "Tune the controller aim-assist behavior. Changes apply after you click Apply.",

    UI_CAA_enable = "Enable aim-assist",
    UI_CAA_enable_tip = "Master toggle. When off, the mod is completely silent and the other settings have no effect.",

    UI_CAA_overlay = "Show snap zones (for screenshots / tuning)",
    UI_CAA_overlay_tip = "Draws colored rectangles around each visible zombie showing the body and head snap zones. NOTE: may affect game balance — recommended for tuning / recording only.",

    UI_CAA_body_w = "Body snap zone width",
    UI_CAA_body_w_tip = "Width of the body snap zone (fraction of zombie sprite height).",
    UI_CAA_body_h = "Body snap zone height",
    UI_CAA_body_h_tip = "Height of the body snap zone, measured upward from the foot.",
    UI_CAA_body_down = "Body snap zone bottom extension",
    UI_CAA_body_down_tip = "Extra downward extension below the foot (covers crawling / prone zombies).",

    UI_CAA_head_w = "Head snap zone width",
    UI_CAA_head_w_tip = "Width of the head snap zone (centered on the head bone projection).",
    UI_CAA_head_h = "Head snap zone height",
    UI_CAA_head_h_tip = "Height of the head snap zone.",
    UI_CAA_head_offset = "Head zone vertical offset",
    UI_CAA_head_offset_tip = "Moves the head snap zone up from the neck bone to the real head center. The game's head bone sits at the neck, so this pushes the zone up to where the head actually is. Turn on 'Show snap zones' and adjust until the cyan zone sits on the head. Follows the zombie's head as it moves.",

    UI_CAA_assist_x = "Horizontal snap strength",
    UI_CAA_assist_x_tip = "How strongly the cursor is pulled toward the zombie horizontally. Higher = stickier. 0.0 = no pull, 1.0 = full lock.",
    UI_CAA_assist_y = "Vertical snap strength",
    UI_CAA_assist_y_tip = "How strongly the cursor is pulled toward the zombie vertically. Higher = stickier.",
    UI_CAA_assist_head = "Head snap strength",
    UI_CAA_assist_head_tip = "Pull strength inside the head snap zone. Higher = stickier. Usually set stronger than body so it's easier to land headshots.",
    UI_CAA_up_resist = "Pull-up easing (body to head)",
    UI_CAA_up_resist_tip = "How much the body snap weakens when you push the stick upward (makes it easier to drag the cursor from the body up to the head). 0 = no easing, 1 = full easing.",

    UI_CAA_track = "Cursor follow strength",
    UI_CAA_track_tip = "How strongly the cursor follows a moving zombie that is being snapped onto. 0 = no follow, 1 = 1:1 follow.",
    UI_CAA_curve = "Stick sensitivity curve",
    UI_CAA_curve_tip = "Per-axis power curve: small stick inputs become slower (finer control), large inputs faster. 1.0 = linear, 1.5 = Steam Relaxed, 2.0 = Wide. If you already use Steam Input curves, keep this at 1.0.",

    UI_CAA_recenter = "Recenter resistance",
    UI_CAA_recenter_tip = "By default the cursor springs back to center when you release the stick — this is what makes you 'fight' to hold a position. Higher values make the cursor resist that pull, so it stays where you left it. 0 = vanilla, 0.9 = nearly stays put. (Still drifts back slowly so aiming exits cleanly when you let go.)",

    UI_CAA_exp_drift = "Enable experimental: Relative drift (recommended)",
    UI_CAA_exp_drift_tip = "Experimental. When on, the cursor gets a 'hold the stick and it keeps sliding' feel (like RDR2 / twin-stick shooters) using the two settings below. Off by default — the two drift settings below stay greyed out until you turn this on.",
    UI_CAA_drift_amount = "Relative drift amount",
    UI_CAA_drift_amount_tip = "How much the cursor keeps drifting while you hold the stick. Higher = more continuous drift, but you lose the 'point exactly where the stick is' precision. Only active when the experimental toggle above is on.",
    UI_CAA_drift_curve = "Drift sensitivity curve",
    UI_CAA_drift_curve_tip = "Stick response curve used while Relative drift is active — drift turns the stick into a speed throttle. Independent from the polar 'Stick sensitivity curve' above. Only active when the experimental toggle above is on.",
}
